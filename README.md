
### Start server

```bash
cd server
# (optional) create and activate a virtualenvironment
pip install -r requirement.py
python server.py
```

### Start serving frontend

```bash
cd frontend
npm install
npm run serve
```

